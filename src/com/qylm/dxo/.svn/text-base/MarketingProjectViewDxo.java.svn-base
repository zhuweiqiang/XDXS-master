package com.qylm.dxo;

import com.qylm.dto.baseSet.MarketingProjectViewDto;
import com.qylm.entity.MarketingProject;

public final class MarketingProjectViewDxo {

	public static void dtoToEntity(MarketingProjectViewDto dto, MarketingProject entity) {
		entity.setProject(dto.getProject());
		entity.setMoney(dto.getMoney());
		entity.setRemark(dto.getRemark());
		entity.setState(dto.isState());
	}

	public static void entityToDto(MarketingProject entity, MarketingProjectViewDto dto) {
		dto.setProject(entity.getProject());
		dto.setMoney(entity.getMoney());
		dto.setRemark(entity.getRemark());
		dto.setState(entity.isState());
	}

}
